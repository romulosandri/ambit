/** @deprecated Import from `@/data` instead. Kept so older local imports keep typechecking. */
export {
  articles,
  briefs,
  chats,
  featuredArticle,
  people,
  publishers,
  todayArticles,
  todayBrief,
  topics,
  user,
  yesterdayArticles,
} from "@/data"
export type { Article as SampleArticle } from "@/data"
